# LTC6820 说明：完全透明的 SPI ↔ isoSPI 网关

## 一句话结论

**LTC6820 是一个纯硬件收发器（Hardware Transceiver），不含任何可编程寄存器，不需要任何软件配置。它完全透明地将标准 SPI 信号转换为 isoSPI 差分信号，反之亦然。**

---

## 为什么工程中没有 LTC6820 相关代码？

不是因为遗漏，而是 LTC6820 **确实不需要任何代码**。它的所有参数都由硬件引脚和外部电阻设定：

| 配置项 | 方式 | 说明 |
|---|---|---|
| Master / Slave 模式 | **MSTR** 引脚 | 接高 = Master，接低 = Slave |
| SPI 模式 (Mode 0~3) | **POL** 和 **PHA** 引脚 | 与 MCU SPI 模式一致即可 |
| 快 / 慢速模式 | **SLOW** 引脚 | 接低 = 1Mbps 全速 |
| 驱动电流 & 信号幅度 | 外部电阻 **RBIAS** | 分压器设定 |
| 接收比较器阈值 | **ICMP** 引脚外接电阻 | 设定判决门限 |
| 逻辑电平 | **VDDS** 供电电压 | 1.7V–5.5V，与 MCU IO 电平匹配 |

**关键：LTC6820 没有 I²C/SPI 配置接口，没有寄存器映射，上电即工作。**

---

## 系统信号流

```
┌──────────┐   SPI (4线)    ┌──────────┐   isoSPI (双绞线)   ┌────────────┐        ┌────────────┐
│  MCU     │ ◄────────────► │ LTC6820  │ ◄────────────────► │ LTC6813[0] │ ◄────► │ LTC6813[1] │ ...
│ Arduino  │  CS/SCK/MOSI/  │ 纯硬件   │   差分脉冲信号      │ 电池监控IC  │        │ 电池监控IC  │
│          │  MISO          │ 透明桥接 │                     │ 有配置寄存器 │        │ 有配置寄存器 │
└──────────┘                └──────────┘                     └────────────┘        └────────────┘
      ↑                          ↑                                ↑
 SPI.transfer()            无需任何代码                   需要写CFGRA/CFGRB
 1MHz, Mode 0, MSB First  引脚strapping决定行为           PEC校验、ADC配置等
```

---

## 工程中仅有的 isoSPI 相关代码 —— 全部针对 LTC6813

本工程中所有提到 "isoSPI" 的地方，都是针对 **LTC6813**（电池监控 IC）的操作，与 LTC6820 无关：

### 1. `wakeup_idle()` — LTC681x.cpp:54

```c
void wakeup_idle(uint8_t total_ic) {
    for (int i = 0; i < total_ic; i++) {
        cs_low(CS_PIN);
        spi_read_byte(0xff);  // 发 dummy byte 唤醒 LTC6813 的 isoSPI 接口
        cs_high(CS_PIN);
    }
}
```

这是 **LTC6813 的协议要求**：每个 LTC6813 在进入 IDLE 状态后，需要接收一个 CS 脉冲 + 时钟信号来唤醒其 isoSPI 端口。不是 LTC6820 需要这个操作。

### 2. `wakeup_sleep()` — LTC681x.cpp:65

```c
void wakeup_sleep(uint8_t total_ic) {
    for (int i = 0; i < total_ic; i++) {
        cs_low(CS_PIN);
        delay_u(300);   // CS 拉低 300µs 唤醒 LTC6813
        cs_high(CS_PIN);
        delay_u(10);
    }
}
```

同样是 **LTC6813 的唤醒时序要求**，LTC6820 只是透明地转发这些信号。

### 3. `isospi_reverse` — LTC681x.h:183

```c
bool isospi_reverse;  // 当 daisy-chain 物理方向反转时标记
```

处理 **多个 LTC6813 在 isoSPI daisy-chain 中的 IC 索引排列顺序**。当链的物理接线方向与默认方向相反时，逻辑 IC[0] 不等于物理第一颗，需要此标志修正数据索引。这是 LTC6813 多芯片级联的应用层逻辑。

> 注意：`isospi_reverse` 的设置与 LTC6820 无关，LTC6820 不知道也不关心链上有几颗 LTC6813。

### 4. `wire_cut` — COMMANDS.h:8

```c
extern int wire_cut;  // isoSPI 断线位置标记（仅声明，未实际使用）
```

---

## 官方来源

- **ADI EngineerZone 论坛**（ADI 工程师官方回复）：
  > "LTC6820 is a hardware transceiver and does not have any programmable/readable register. You can send SPI commands, it will convert them into isoSPI and vice versa."

- **LTC6820 数据手册**：
  > "Requires No Software Changes in Most SPI Systems."

- 数据手册链接：[LTC6820 Datasheet (Analog Devices)](https://www.analog.com/en/products/ltc6820.html)

---

## 常见误区

| 误区 | 事实 |
|---|---|
| LTC6820 需要通过 SPI 写寄存器配置 | ❌ 没有任何寄存器，引脚 strapping 确定一切 |
| isoSPI 协议需要特殊软件处理 | ❌ 对 MCU 来说就是普通 SPI，LTC6820 透明转换 |
| 工程没写 LTC6820 代码是 bug | ❌ 根本不需要写，写了反而是多余的 |
| `wakeup_idle()` 是唤醒 LTC6820 | ❌ 发 0xFF 是唤醒 LTC6813 的 isoSPI 端口 |
| `isospi_reverse` 和 LTC6820 有关 | ❌ 仅用于多颗 LTC6813 的 daisy-chain 寻址 |

---

## 总结

**LTC6820 = SPI 转 RS485 的 MAX485 之于 UART 一样 —— 纯电平转换/物理层桥接，软件零感知。** 如果你的 SPI 通信有问题，应该检查：

1. LTC6820 的引脚 strapping 是否正确（MSTR、POL、PHA、SLOW）
2. RBIAS 电阻值是否合适
3. 脉冲变压器和端接电路是否正常
4. 线缆长度和质量是否在规格范围内（最长 100m CAT-5）

而不是去怀疑"代码里是不是少了 LTC6820 的初始化"。
